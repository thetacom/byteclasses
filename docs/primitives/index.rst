Primitives
==========

Primitive ``byteclasses`` designed to be used independently or used to compose larger custom ``byteclasses``.

Each primitive ``byteclass`` has a fixed ``byte_length`` with a configurable ``byte_order``.

.. toctree::

   generics
   integers
   floats
   characters
   bitfields
