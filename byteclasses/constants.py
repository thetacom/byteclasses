"""Byteclass Constants Module."""

_BYTECLASS = "__byteclass__"

# The name of an attribute on the collection class where we store the Member objects.
# Also used to check if a class is a byteclass collection class.
_MEMBERS = "__collection_members__"

# The name of an attribute on the class that stores the parameters to
# fixed collection.
_PARAMS = "__collection_params__"
